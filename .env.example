# Copy to .env. The Hugging Face token is the only required setting; it is
# needed for speaker diarization. Everything else (source, language, model,
# device, etc.) is chosen in the web UI per job.
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
